---
"$schema": http://json-schema.org/draft-04/hyper-schema
title: 特定ユーザーの投稿
description: 特定ユーザーの投稿に関するAPI
stability: prototype
strictProperties: true
type:
- object

links:
- title: user's posts
  description: Show a list of a user's posts
  href: /v1/{user_id}/posts
  method: GET
  rel: instances
  schema:
    type: object
    properties:
      limit:
        description: limit
        example: 10
        type: integer
      cursor:
        anyOf:
          - $ref: /schemata#/datetime
          - type: "null"
  targetSchema:
    $ref: /schemata/post#/definitions/post_page

- title: あるユーザーの投稿の内訳
  description: Show a breakdown of user's posts
  href: /v1/{user_id}/posts/hashtag_breakdown
  method: GET
  rel: show
  schema:
    type: "null"
  targetSchema:
    type: object
    required:
      - data
    properties:
      data:
        type: array
        items:
          $ref: /schemata/hashtag#/definitions/breakdown

id: schemata/v1_users_posts
