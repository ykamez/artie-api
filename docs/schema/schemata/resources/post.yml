---
"$schema": http://json-schema.org/draft-04/hyper-schema
title: Post
description: Post Resource
stability: prototype
strictProperties: true
type:
- object
definitions:
  id:
    description: unique identifier of post
    type: integer
    example: 1
    readOnly: true
  text:
    description: post's text
    type: string
    example: "Pig Coin is awesome."
  image_data:
    description: images attached to post
    type: string
    example: 'aws:pig_coin.png'
  published_at:
    description: datetime when this post was posted.
    type: string
    format: date-time
  share:
    description: post share count
    type: integer
    example: 123

  post:
    type: object
    required:
      - id
      - text
      - user
      - published_at
    properties:
      id:
        $ref: /schemata/post#/definitions/id
      text:
        $ref: /schemata/post#/definitions/text
      user:
        $ref: /schemata/user#/definitions/user
      image_data:
        type: array
        items:
          $ref: /schemata/post#/definitions/image_data
      hashtag:
        type: array
        items:
          $ref: /schemata/hashtag#/definitions/hashtag
      share:
        $ref: /schemata/post#/definitions/share
      post_evaluation:
        $ref: /schemata/post_evaluation#/definitions/post_evaluation
      published_at:
        $ref: /schemata/post#/definitions/published_at
  post_page:
    type: object
    required:
      - data
      - paging
    properties:
      data:
        type: array
        items:
          $ref: /schemata/post#/definitions/post
      paging:
        $ref: /schemata/paging#/definitions/paging
      message:
        $ref: /schemata#/mesasge

id: schemata/post
